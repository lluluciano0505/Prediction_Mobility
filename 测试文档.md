# 测试文档

## 目标

验证两件事：

1. VectorEngine API 能否稳定调用。
2. 现有 HuMob 数据集是否能转换为可用于向量化的输入，并产出有效向量。

## 安全要求

1. 不要把 API Key 写入代码、脚本或提交到 Git。
2. 仅通过环境变量注入密钥：VECTORENGINE_API_KEY。
3. 如果密钥曾在公开渠道出现，建议立即在服务端吊销并重建。

## 理论可行性结论

结论：可行。

理由：

1. 你当前 API 是 OpenAI 兼容协议，Python SDK 可直接接入。
2. 现有 OD 表格数据可以序列化为文本片段，再送入 embedding 接口。
3. 向量结果可作为后续新闻/地理语义特征管道的一部分，用于 Model C（结构化 + 向量）。

## 已提供测试脚本

脚本路径：src/vectorengine_usability_test.py

能力：

1. chat 模式：最小联通性验证。
2. embedding 模式：从数据集抽样并请求向量，返回维度、样本成功率、向量范数统计。

## 快速测试步骤

### 1) 安装依赖

pip install -r requirements.txt

### 2) 设置环境变量（本地终端执行）

export VECTORENGINE_API_KEY="你的密钥"

### 3) 先做联通性 smoke test

python -m src.vectorengine_usability_test \
  --data-path data/humob2026-dataset.tsv \
  --mode chat \
  --model gpt-5.5-pro

成功判定：返回 JSON 中 ok=true。

### 4) 做向量可用性测试

python -m src.vectorengine_usability_test \
  --data-path data/humob2026-dataset.tsv \
  --mode embedding \
  --model text-embedding-3-large \
  --sample-size 64

如果你的服务侧 embedding 模型名不同，请替换 --model。

## 可用性验收标准

1. API 调用成功率 >= 95%（抽样阶段可先按 64 条）。
2. embedding_dim > 0 且样本条数一致。
3. 向量范数统计稳定（无全零向量、无大面积异常值）。
4. 失败请求具备可定位错误信息（模型名错误、配额、网络超时等）。

## 下一步建议

1. 把新闻数据也做同样的 embedding 抽样测试。
2. 将向量按 date x cell 聚合（均值/加权均值），并加入现有训练管道。
3. 做 A/B 对比：
   - A: 时序+周期
   - B: A+新闻结构化
   - C: B+新闻向量
